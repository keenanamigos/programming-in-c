#include <stdio.h>
// stdio.h is a header file and should be included in just about every C program
int main(void)
{
    printf("This is my first C program (but not really)!\n");

    return 0; // Convention to indicate that the program has complete successfully
}